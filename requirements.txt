# minimum versions, picked so a plain `pip install -r requirements.txt` works
# on Python 3.11 through 3.13 (older exact pins had no wheels for 3.13 and
# tried to compile from source).
gspread>=6.1.2
pandas>=2.2.3
psycopg2-binary>=2.9.10
sqlalchemy>=2.0.36
fastapi>=0.115.0
uvicorn>=0.30.0
apscheduler>=3.10.4
python-dotenv>=1.0.1
rich>=13.7.1
